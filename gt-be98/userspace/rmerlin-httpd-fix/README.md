# GT-BE98 RMerlin 102.9: physical-trial corrections

This checkpoint follows `rmerlin-ipsec-fix-20260916`, which in turn follows published merge `dc72164482512481a62517f0f14e86c2fa3cf776`. Restore those checkpoints and the separate A53 toolchain/sysroot before replaying these scripts. No toolchain binaries are vendored into this repository. Production code is ARMEL softfp with glibc 2.44, GCC 15.2, `-mcpu=cortex-a53+crypto -march=armv8-a+crc+crypto -mfpu=crypto-neon-fp-armv8 -mfloat-abi=softfp`; the builds run on the DGX, never on the router. GNU SHA-1 build IDs are retained.

The first image (a1) aborted in rc while setting up IPsec and automatically returned to committed slot 2 / #35. The IPsec correction uses IFNAMSIZ and checked copies for an interface such as `eth0` including its terminator. The next image (a2) booted correctly, but its web translation function aborted under GCC 15 FORTIFY_SOURCE=3 when replacing a domain tag. A private SIGABRT stack capture maps the fortified call to `replace_tag_string`, ej.c:191. Only the symbolized result is published; raw process memory, router credentials, NVRAM and boot journals are excluded.

The HTTPD correction bounds prefix, replacement and tail copies by the remaining capacity, preserves the original text when replacement cannot fit, skips empty search strings and handles empty replacements. The QEMU user-mode regression reproduces the old SIGABRT and checks repeated tags, exact-fit output, overflow, small destination and deletion with canaries. Memory checks remain enabled.

Only `ej.o` is rebuilt and HTTPD relinked against the reviewed existing inputs. The actual source fix is also under `release/src/router/httpd/ej.c`. Build with `scripts/build.py`, then `scripts/prepare-root.py`, after applying `patches/httpd-translation-bounds.patch` to the parent source tree. The final rootfs starts from a2, adds the stripped HTTPD and identifies itself as 3006.102.9-beta1-leon3. The kernel, signed bootfs, modules, TLS ABI separation and systemd compatibility remain unchanged.

Physical RAM validation on a2 with this HTTPD passed login, index, system information, OpenVPN and QoS pages. Docker passed DNS, HTTP, certificate-verified HTTPS, custom bridge DNS, a LAN published port and 32 MiB memory/memory+swap limits. Temporary containers/networks were removed. A test fixture now derives the actual UID 0 username instead of assuming `root`; the renamed ASUS administrator account is valid. The web test uses the OpenVPN page actually present in the image.

Run `scripts/pack-rehearsal.py --revision httpd-fix-v1`, `scripts/run-qemu.py` with the saved Image36 kernel, then `scripts/pack.py --revision httpd-fix-v1`. Final a3 packaging and physical trial results are recorded below. Firmware commit is intentionally separate from Git source commits.

QEMU `httpd-fix-v1` passed in 106.4 s with normal unmount and poweroff. Candidate `GT-BE98_leon36-rmerlin1029a3_zstd22.pkgtb` is 90,840,140 bytes; SHA256 `8cd5b0c8230a1c0aa5ca91a0f159351bea726ddbb90b07a19cc6d00cf372e73b`. SquashFS uses zstd 22 / 1 MiB blocks / tailends; reserved bootfs/rootfs is 107+626 of 734 LEB. The signed bootfs and all other FIT payloads are byte-identical. Physical a3 trial passed after the inactive-slot write and complete payload readback. The runtime checked 263 file hashes, systemd ownership and notification, four radios, the running OpenVPN server, TLS/DNS tools, authenticated management pages and Docker network/memcg. HTTPS management transport negotiated TLS 1.3; certificate trust was not evaluated. Stable hardware flows advanced by 3,832 hits / 1,122,617 bytes over 20 seconds. No new abort, kernel panic or OOM was found in the current boot logs. Key network/acceleration NVRAM values are unchanged. This AP-mode check is not a WAN line-rate benchmark or validation of new hardware AQM.

The RAM-only acceptance file prevents the 900-second trial timer from rebooting this healthy session. It does not commit firmware: booted slot 1, normal reboot slot 2, commit flags 0/1. A normal reboot returns to the committed #35 fallback. Docker is running under `/usr/local` with data on the USB-backed `/jffs/docker/data`; only labelled test containers and networks were removed. Readback hashes confirm the fallback volumes and loader are unchanged.

The management UI reports `buildno=102.9` and `extendno=beta1-leon1_rog`; the a3 candidate identity is recorded in `/usr/share/leon-upstream.json`. Fields such as `flashed: false` in immutable build manifests describe packaging time; the later physical receipt is `flash/evidence/trial-result.json`.
